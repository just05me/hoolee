# Скопируйте в .env на сервере (/opt/hoolee/.env). Файл .env в git не попадает.
BOT_TOKEN=          # токен из @BotFather
CHAT_ID=            # ваш chat id: python3 services/api/server.py --chat-id
ALLOWED_ORIGIN=https://hoolee.uz
